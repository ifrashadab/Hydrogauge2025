name: hydrogauge
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.11.0+2
  geolocator: ^12.0.0
  geocoding: ^2.2.0        # ✅ Added for address lookup
  intl: ^0.19.0
  path_provider: ^2.1.3
  mobile_scanner: ^4.0.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  table_calendar: ^3.1.2
  url_launcher: ^6.3.1
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/login_bg.jpg